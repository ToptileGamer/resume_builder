{
  "name": "resume-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/login.js --bundle --outfile=login.bundle.js --format=iife --platform=browser && esbuild src/auth-guard.js --bundle --outfile=auth-guard.bundle.js --format=iife --platform=browser"
  },
  "dependencies": {
    "@netlify/identity": "*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
